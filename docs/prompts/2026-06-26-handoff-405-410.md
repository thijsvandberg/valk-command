# Handoff prompt — BRDG-405 → BRDG-410 (autonomous)

Generated by `/handoff all` on 2026-06-26 from the post-refactor re-audit thread. Copy the fenced
block below into a fresh Claude Code thread in the `valk-command` repo to run it.

```text
You are implementing stories BRDG-405 → BRDG-410 (the six items from the 2026-06-25 post-refactor
re-audit) end-to-end, AUTONOMOUSLY, one after another, on the `dev` branch of the Bridge
(valk-command) project. This prompt is your standing approval to implement all of them without
pausing to ask "shall I start?" per item. Commit your own work as you go, track progress, archive
finished stories, and only advance once the current item is genuinely green. Keep going until all six
are done or you hit a real blocker.

Read first:
- `CLAUDE.md` (project + global rules).
- `docs/investigations/2026-06-25-refactor-reaudit.md` — why these stories exist; READ the "LRU
  freeze" note (it governs BRDG-410).
- `docs/architecture/optimistic-updates.md` — the optimistic-edit overlay. BRDG-405/406/407 must NOT
  break it.
- Each story's file in full before you start that story.

Scope & order (do them in this order — lowest-risk first):
1. BRDG-409 — route input-validation hardening (encode/validate dynamic path params, parseJsonBody on
   the last two ad-hoc routes). Isolated, backend, clear pass/fail.
   `docs/user-stories/BRDG-409-route-input-validation-hardening.md`
2. BRDG-410 — polling & memory hygiene (cap revalidation-queue, fix interval/unmount leaks, visibility
   gating). `docs/user-stories/BRDG-410-polling-and-memory-hygiene.md`
   ⚠ Do NOT touch the LRU SWR fix in `swr-lru-provider.ts`. Cap `revalidation-queue.lastChecked` by
   pruning a SNAPSHOT of the Map, never by iterating-and-mutating the live Map (that is what caused
   the recent app freeze when editing descriptions).
3. BRDG-408 — finish the Jira sync N+1 (bulk the tranche sync path, chunk getIssuesByKeys, bulk the
   rank timestamp refresh) and reconcile BRDG-378's checklist.
   `docs/user-stories/BRDG-408-finish-jira-sync-n-plus-one.md`
4. BRDG-407 — Compare view (MultiSprintView) onto the optimistic overlay so edits stop snapping back.
   Isolated to `MultiSprintView.tsx`. `docs/user-stories/BRDG-407-compare-view-optimistic-overlay.md`
5. BRDG-406 — finish the row-actions convergence (board adopts the shared `useRowActions` glue instead
   of its local copies). Heavy edits to `SprintBoard.tsx`.
   `docs/user-stories/BRDG-406-finish-row-actions-convergence.md`
6. BRDG-405 — sprint board render performance (stop the per-row re-render fan-out, move
   setRouterPrefetch into an effect, snapshot the drag list). Touches `SprintBoard.tsx` +
   `TicketTable.tsx`. `docs/user-stories/BRDG-405-board-render-performance.md`
⚠ Coupling: BRDG-406 and BRDG-405 both edit `SprintBoard.tsx`. Do them sequentially in this order
(406 before 405) and re-run the FULL test suite after each so a shared-file change cannot silently
regress the other. Do not work on them in parallel.

The loop for each item:
1. Read the story. Each has an "Open Questions" section WITH a recommended default — follow the
   recommendation; do not stop to ask unless the default proves wrong/blocking once you're in the code.
2. Implement it. You may use the `/implement-story` command as the per-story harness. These are
   refactors/hardening: PRESERVE existing behaviour (the tests are your guardrail); do not change
   behaviour beyond the story's scope.
3. If anything visual changes, invoke the `frontend-design` skill first (global rule). Most of these
   are non-visual refactors, so it usually won't apply — honour the rule if visuals move.
4. Write/extend co-located `*.test.ts(x)` tests for every change (cover the story's "Tests" list).
5. Verify it ACTUALLY works (the bar to advance — see "Definition of done").
6. Commit (conventional commit referencing the story ID; English; no emojis; NO "Co-Authored-By").
   Stage EXPLICIT paths only — never `git add -A`/`.` (the tree may carry unrelated work). Several
   commits per story is fine.
7. Tick the story's checkboxes as you satisfy them; when all are met, add a short "## Status" run note
   at the top of the story and archive it: move the file to `docs/user-stories/completed/` and commit
   `chore: archive BRDG-XXX as completed`.
8. Only then move to the next story.

Definition of done (must ALL hold before advancing past a story):
- `npm run lint`, `npm run typecheck`, `npx vitest run` (FULL suite), and `npm run build` all green.
- The story's acceptance criteria are met.
- It is E2E-verified in the REAL running app — not just unit tests:
  - UI stories (407, 406, 405): drive the affected view in Chrome via the available browser tools;
    confirm the behaviour (no snap-back, drag lands correctly, menu/bulk actions fire) and that the
    console shows no new errors. Navigate from the sprint board (click into a ticket), NOT by direct
    URL (direct URLs hit the Clerk redirect). Use the `verify` or `validate-ui` skill.
  - Backend stories (409, 408, 410): trigger the real route/sync against the running dev server and
    confirm correct behaviour + no server errors, in addition to the unit tests.

Hard rules (non-negotiable):
- TESTS: `npx vitest run` in the FOREGROUND, ONE process at a time, no pipes, no background, no
  sleep+cat polling. 16GB machine — concurrent vitest thrashes swap. A postToolUse hook may auto-run
  tests after edits; let it finish, do not launch an overlapping run.
- Run lint + typecheck + test + build before EVERY commit.
- DEV SERVER on port 3100 for Chrome checks: `curl -s localhost:3100` to see if it's up before
  starting; never start a second instance; never background it with `&`. After any `npm run build`,
  RESTART it (`lsof -ti:3100 | xargs kill -9 2>/dev/null` then `npm run dev`).
- BRANCHES: do NOT create/switch branches (a PreToolUse hook blocks it). Commit directly to `dev`.
  No PR unless asked.
- Conventional commits, English only, no emojis, no "Co-Authored-By". Update `/docs` when
  behaviour/architecture changes (e.g. optimistic-updates.md if 406/407 change the overlay's shared use).

Chrome / auth: the app is Clerk-gated. First check existing browser tabs and reuse an
already-authenticated Bridge tab (localhost:3100) if one is open; otherwise use the development auth
bypass (check `src/middleware.ts` and `src/app/api/dev/` / `src/app/dev/` — a dev-only httpOnly
cookie). If neither works, that's a valid reason to ask the PO to log in once in the tab you're
driving. Never trigger native alert/confirm dialogs (they freeze the automation).

Ask the PO sparingly: decide for yourself using each story's recommended defaults and the tests. Only
ask when something is genuinely unverifiable by tests or Chrome (a subjective product/visual call, a
wrong default that changes scope, or you cannot authenticate Chrome). BATCH questions into one
message; keep working on anything unblocked; never ask permission to start/continue.

Pause, don't thrash: if after a couple of honest attempts a story won't go green, or a shared-file
change regresses unrelated tests and the fix is non-obvious, STOP that story, leave the tree
committed-or-clean (never a half-applied broken edit), note where you're stuck, and continue with the
next INDEPENDENT story (409/410/408/407 are independent; 405 depends on 406 being sound). Report
blocked stories at the end. Never loop indefinitely on the same failure.

At the end, report a summary in Dutch, understandable for a technical PO, concise and to the point:
which stories shipped (with commit hashes), which are blocked and why, anything deferred or worth the
PO's attention. No long prose.

Begin with BRDG-409. Work autonomously through to BRDG-405.
```
