# Handoff — resterend werk re-audit follow-ups (BRDG-417, BRDG-415, BRDG-416)

Paste the block below into a fresh thread to run it autonomously.

```text
You are implementing the remaining 2026-06-25 re-audit follow-ups — stories BRDG-417, BRDG-415,
BRDG-416 (in that order) — end-to-end, AUTONOMOUSLY, one after another, on the `dev` branch of the
Bridge (valk-command) project. This prompt is your standing approval to implement all of them without
pausing to ask "shall I start?" per item. Commit your own work as you go, track progress, archive
finished stories, and only advance once the current item is genuinely green. Keep going until all are
done or you hit a real blocker.

Read first:
- `CLAUDE.md` (project + global rules).
- `docs/investigations/2026-06-25-refactor-reaudit.md` — the audit that motivates these items; READ
  the "LRU freeze" note (do NOT touch `swr-lru-provider.ts`).
- `docs/architecture/optimistic-updates.md` — the pending-edits overlay. BRDG-415/416 touch the board;
  do NOT break it.
- The parent stories for context (already shipped/archived): `docs/user-stories/completed/BRDG-405-*`,
  `BRDG-406-*`, `BRDG-408-*`, `BRDG-410-*` — each has a "## Status" note explaining what shipped and
  why the remainder was deferred. The three stories below are those remainders.
- Each story's file IN FULL before you start it (they carry the trade-offs + recommended approach).

Scope & order (do them in this order — independent/lowest-risk first):
1. BRDG-417 — re-audit long-tail hygiene (independent, Low priority, no board coupling): burnup-seed
   N+1, `useConversations`/`useMessages` → SWR, event-bus + RefinementSessionContext edges.
   `docs/user-stories/BRDG-417-reaudit-longtail-sync-polling-hygiene.md`. Each sub-item is independent;
   if one won't go green, ship the others and report it. The RefinementSessionContext timer's
   post-unmount write is probably WANTED (it persists session position) — decide before changing.
2. BRDG-415 — finish the board's row-actions glue convergence (High): the board adopts the shared
   `useRowActions` glue (rowMenu/context-menu, quick-moves, create-sprint, flag-state, copy, refine),
   splits `ticket-action-menu.tsx`, and prunes the selection.
   `docs/user-stories/BRDG-415-finish-board-row-actions-glue-convergence.md`.
   Approach (from the story): EXTEND `useRowActions` options (`onContextMenuOpen`,
   `onConfirmQuickCreate`; rich move-toast already rides `onMove`) rather than keep board copies; keep
   `handleRankToEdge` board-local; the bulk write/move/flag primitives ALREADY route through the hook
   — do not re-route those. Selection pruning must use adjust-state-during-render (setState-in-effect
   is build-blocking under React Compiler).
3. BRDG-405's render remainder → BRDG-416 — sprint board per-row render fan-out + virtualizer offset +
   header extract (High). `docs/user-stories/BRDG-416-board-render-fanout-and-virtualizer.md`.
   CAUTION baked into the story: do NOT apply the audit's "pass the Sets down" suggestion — handing
   `BoardRow` the Sets gives a new Set identity per toggle and breaks its shallow `memo` for ALL rows
   (makes it worse). The props are already individual booleans; build a render-count harness FIRST,
   MEASURE which interaction actually fans out, then stabilise the offending map/handler reference.

Coupling & cautions (non-negotiable for this batch):
- BRDG-415 and BRDG-416 BOTH heavily edit `src/components/sprint-board/SprintBoard.tsx`. Do them
  SEQUENTIALLY (415 before 416), NEVER in parallel, and re-run the FULL test suite between them so a
  shared-file change cannot silently regress the other.
- BRDG-417 is independent of the board (burnup route, chat hooks, event-bus, refinement context).
- Parallel work is in flight in this repo (a separate "active-sprint status changes" effort, ~BRDG-414)
  touching `jira-client.ts`/`jira-client.test.ts`, `src/app/(app)/inbox/page.tsx`, and
  `src/app/dev/exploration/` + `src/app/api/.../status-changes`. The working tree therefore carries
  unrelated changes: stage EXPLICIT paths only, and do NOT revert or commit edits you did not make.
- Do NOT touch `swr-lru-provider.ts` (the LRU SWR fix) or the `pendingTicketEdits` overlay beyond what
  a story explicitly scopes.

The loop for each item:
1. Read the story. Each "Open Questions" section has a recommended default — follow it; do not stop to
   ask unless the default proves wrong/blocking once you're in the code.
2. Implement it. You may use the `/implement-story` command as the per-story harness. These are
   refactors / perf / hygiene: PRESERVE existing behaviour unless the story says otherwise; the tests
   are your guardrail; do not change behaviour beyond the story's scope.
3. If anything visual changes, invoke the `frontend-design` skill first (global rule). These are
   mostly non-visual (structure/perf/hygiene), so it usually won't apply — honour the rule if visuals
   move.
4. Write/extend co-located `*.test.ts(x)` tests for every change (cover the story's "Tests" list).
   BRDG-416 specifically needs a render-count test (toggling one row re-renders only that row).
5. Verify it ACTUALLY works (the bar to advance — see "Definition of done").
6. Commit (conventional commit referencing the story ID; English; no emojis; NO "Co-Authored-By").
   Stage EXPLICIT paths only — never `git add -A`/`.`. Several commits per story is fine.
7. Tick the story's checkboxes as you satisfy them; when all are met, add a short "## Status" run note
   at the top and archive it: move the file to `docs/user-stories/completed/` and commit
   `chore: archive BRDG-XXX as completed`.
8. Only then move to the next item.

Definition of done (must ALL hold before advancing past a story):
- `npm run lint`, `npm run typecheck`, `npx vitest run` (FULL suite), and `npm run build` all green.
- The story's acceptance criteria are met.
- It is E2E-verified in the REAL running app — not just unit tests:
  - UI items (BRDG-415 menu/bulk-bar; BRDG-416 board render): drive the affected view in Chrome via
    the available browser tools; confirm the behaviour (context menu fires, no snap-back, drag lands,
    board renders) and that the console shows no new errors. Navigate from the sprint board (click
    into a ticket), NOT by direct URL (direct URLs hit the Clerk redirect). Use the `verify` or
    `validate-ui` skill.
  - Backend/hook items (BRDG-417): trigger the real route/sync against the running dev server and
    confirm correct behaviour + no server errors, in addition to the unit tests.

Hard rules (non-negotiable):
- TESTS: `npx vitest run` in the FOREGROUND, ONE process at a time, no pipes, no background, no
  sleep+cat polling. 16GB machine — concurrent vitest thrashes swap. A postToolUse hook may auto-run
  tests after edits; let it finish, do not launch an overlapping run.
- Run lint + typecheck + test + build before EVERY commit.
- DEV SERVER on port 3100 for Chrome checks: `curl -s localhost:3100` to see if it's up before
  starting; never start a second instance; never background it with `&`. After any `npm run build`,
  RESTART it (`lsof -ti:3100 | xargs kill -9 2>/dev/null` then `npm run dev`).
- BRANCHES: do NOT create/switch branches (a PreToolUse hook blocks it). Commit directly to `dev`.
  No PR unless asked.
- Conventional commits, English only, no emojis, no "Co-Authored-By". Update `/docs` when
  behaviour/architecture changes (e.g. optimistic-updates.md if the board's shared use changes).

Chrome / auth: the app is Clerk-gated. First check existing browser tabs and reuse an
already-authenticated Bridge tab (localhost:3100) if one is open; otherwise use the development auth
bypass (check `src/middleware.ts` and `src/app/api/dev/` / `src/app/dev/` — a dev-only httpOnly
cookie acquired via `GET /api/dev/bypass`; note evaluate_js returns from async bodies don't serialise,
so stash results on `window` and read them in a second call). If neither works, that's a valid reason
to ask the PO to log in once in the tab you're driving. Never trigger native alert/confirm dialogs
(they freeze the automation). For E2E mutations prefer safe/local fields (e.g. PO metadata like
businessValue) over real Jira writes on production tickets; the dedicated test story is VPL-1337.

Ask the PO sparingly: decide for yourself using each story's recommended defaults and the tests. Only
ask when something is genuinely unverifiable by tests or Chrome (a subjective product/visual call, a
wrong default that changes scope, or you cannot authenticate Chrome). BATCH questions into one
message; keep working on anything unblocked; never ask permission to start/continue.

Pause, don't thrash: if after a couple of honest attempts a story won't go green, or a shared-file
change regresses unrelated tests and the fix is non-obvious, STOP that story, leave the tree
committed-or-clean (never a half-applied broken edit), note where you're stuck, and continue with the
next INDEPENDENT story. BRDG-417 is independent; BRDG-415 and BRDG-416 share `SprintBoard.tsx` (415
must be sound before 416). Never loop indefinitely on the same failure.

At the end, report a summary in Dutch, understandable for a technical PO, concise and to the point:
which stories shipped (with commit hashes), which are blocked and why, anything deferred or worth the
PO's attention. No long prose.

Begin with BRDG-417. Work autonomously through to BRDG-416.
```
