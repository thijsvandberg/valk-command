# Handoff prompt — Epic Writer BRDG-478 + BRDG-484

Paste the block below into a fresh Claude Code thread in the `valk-command` project to run autonomously.

```text
You are implementing the Epic Writer follow-up stories BRDG-478 (small polish) then BRDG-484 (layout, navigation & content views) end-to-end, AUTONOMOUSLY, one after another, on the `dev` branch of the Bridge (valk-command) project. This prompt is your standing approval to implement both without pausing to ask "shall I start?" per item. Commit your own work as you go, track progress, archive finished stories, and only advance once the current item is genuinely green. Keep going until both are done or you hit a real blocker.

Read first: `CLAUDE.md` (project + global rules); both story files in full before you start each one; `docs/user-stories/completed/BRDG-479-epic-writer-advance-to-breakdown.md` for context on what JUST shipped (the breakdown flow) — you must not regress it; `docs/architecture/ui-primitives.md` (shared issue pill + ToastCard/Tooltip, needed for BRDG-478); `docs/architecture/story-writer.md` (the multi-app/pane system in `src/components/story-writer/panes/`, needed for BRDG-484 #1); and `docs/architecture/optimistic-updates.md` only if you end up touching board edits.

Scope & order (do them in this order):
1. BRDG-478 — Epic Writer small polish — `docs/user-stories/BRDG-478-epic-writer-misc-improvements.md`. Five small, mostly independent fixes: (1) carry the creation description into the writer, (2) visible Save draft / Push to Jira feedback, (3) suppress the empty assistant bubble + decide on related-stories, (4) header epic key as the shared issue pill, (5) strip `<epic-breakdown>`/`<epic-questions>`/`<story-detail>`/`<sprint-plan>` from the chat display (display-only; apply-output still parses the raw output).
2. BRDG-484 — Epic Writer layout, navigation & content views — `docs/user-stories/BRDG-484-epic-writer-layout-navigation.md`. Three parts: (1) view the saved epic draft inside the writer by reusing the Story Writer pane/app system, (2) resizable chat/breakdown split + make the two panes scroll INDEPENDENTLY (root cause noted in the story: the grid columns are plain block divs so the inner overflow-y-auto is not height-bounded and the page scrolls instead), (3) reconcile the phase rail + sidebar CTA + header actions into one coherent progression model.

Couplings & cautions (respect these exactly):
- BRDG-478 and BRDG-484 BOTH edit `src/components/epic-writer/EpicWriterLayout.tsx` (478: header issue pill + save/push feedback wiring; 484: panel layout/scroll/navigation). Do them SEQUENTIALLY — finish 478 fully (green + committed + archived) BEFORE starting 484 — and re-run the FULL suite between them. Never work them in parallel.
- BRDG-478 items (3) and (5) edit `src/components/story-writer/ChatMessageParts.tsx`, which is SHARED with the single-story Story Writer. Your empty-bubble suppression and epic-tag stripping must not change Story Writer rendering — verify the Story Writer chat (`/tickets/<key>/write`) still renders drafts/related/normal messages correctly.
- DO NOT touch the BRDG-479 work that just shipped: the fresh `break-down-epic` skill dispatch and the dedup `datetime()` fix in `src/lib/story-writer-messages.ts`. Leave that logic intact; only add to it if a story explicitly requires it.
- BRDG-484 #1 must REUSE `src/components/story-writer/panes/` (WriterContext + apps), not fork a lookalike, and must not regress the Story Writer's own pane behaviour.

The loop for each item:
1. Read the story. If it has an "Open Questions"/recommended-default section, follow the recommendation — do not stop to ask unless the default proves wrong/blocking once you're in the code. BRDG-478 is a task checklist — do every task; BRDG-484 has a phase-navigation design choice (task 3): if that is a genuine subjective product call you cannot settle from the code, batch it as a PO question but keep implementing the parts you can (content view, resizable + independent scroll).
2. Implement it. You may use the `/implement-story` command as the per-story harness. These are polish/features: preserve existing behaviour unless the story says otherwise; the tests are your guardrail; do not change behaviour beyond scope.
3. If anything visual changes, invoke the `frontend-design` skill first (global rule). BRDG-484 is layout/interaction work — always invoke it there.
4. Write/extend co-located `*.test.ts(x)` tests for every change.
5. Verify it ACTUALLY works (the bar to advance — see "Definition of done").
6. Commit (conventional commit referencing the story ID; English; no emojis; NO "Co-Authored-By"). Stage EXPLICIT paths only — never `git add -A`/`.` (the tree carries unrelated parallel work, including some deleted story docs — do not stage those). Several commits per story is fine.
7. Tick the story's checkboxes as you satisfy them; when all are met, add a short "## Status" run note at the top and archive the story: move it to `docs/user-stories/completed/` and commit `chore: archive BRDG-XXX as completed`.
8. Only then move to the next item.

Definition of done (must ALL hold before advancing):
- `npm run lint`, `npm run typecheck`, `npx vitest run` (FULL suite), and `npm run build` all green.
- The story's acceptance criteria are met.
- It is E2E-verified in the REAL running app — not just unit tests:
  - UI items: drive the Epic Writer in Chrome via the available browser tools, confirm the behaviour and that the console shows no new errors. Navigate from the sprint board (click into an epic / open the epic writer), not by direct URL (direct URLs hit the Clerk redirect). Use the `verify` or `validate-ui` skill. For the shared ChatMessageParts changes, also spot-check the single-story Story Writer.
  - Backend items: trigger the real route/sync against the running dev server and confirm correct behaviour + no server errors.

Hard rules (non-negotiable):
- TESTS: `npx vitest run` in the FOREGROUND, ONE process at a time, no pipes, no background, no sleep+cat polling. 16GB machine — concurrent vitest thrashes swap. A postToolUse hook may auto-run tests after edits; let it finish, don't overlap it.
- Run lint + typecheck + test + build before EVERY commit.
- DEV SERVER on port 3101 for Chrome checks (prod is 3100): `curl -s localhost:3101` to see if it's up before starting; never start a second instance; never background it with `&`. After any `npm run build`, RESTART it (`lsof -ti:3101 | xargs kill -9 2>/dev/null` then `npm run dev`).
- BRANCHES: do NOT create/switch branches (a PreToolUse hook blocks it). Commit directly to `dev`. No PR unless asked.
- Conventional commits, English only, no emojis, no "Co-Authored-By". Update `/docs` when behaviour/architecture changes.

Chrome / auth: the app is Clerk-gated. First check existing browser tabs and reuse an already-authenticated Bridge tab if one is open; otherwise use the development auth bypass (`GET /api/dev/bypass` on the dev server; check `src/middleware.ts` and `src/app/api/dev/` / `src/app/dev/` — a dev-only httpOnly cookie). Epic Writer lives at `/epics/<key>/write`; `VPL-47279` is a real epic with an active session and a populated breakdown. If auth can't be established, that's a valid reason to ask the PO to log in once in the tab you're driving. Never trigger native alert/confirm dialogs (they freeze the automation).

Ask the PO sparingly: decide for yourself using the story's recommended defaults and the tests. Only ask when something is genuinely unverifiable by tests or Chrome (a subjective product/visual call — most likely the BRDG-484 phase-navigation model — a wrong default that changes scope, or you can't authenticate Chrome). BATCH questions into one message; keep working on anything unblocked; never ask permission to start/continue.

Pause, don't thrash: if after a couple of honest attempts an item won't go green, or a shared-file change (ChatMessageParts.tsx / EpicWriterLayout.tsx) regresses unrelated tests and the fix is non-obvious, STOP that item, leave the tree committed-or-clean (never a half-applied broken edit), note where you're stuck, and continue with the next INDEPENDENT item. Report blocked items at the end. Never loop indefinitely on the same failure.

When every item is done: archive this handoff prompt itself — `git mv docs/prompts/2026-07-06-epic-writer-478-484.md docs/user-stories/completed/` and commit `chore: archive handoff prompt epic-writer-478-484`. This keeps `docs/prompts/` to active handoffs only. Skip this if any item is still blocked (leave the prompt in place so the next run can pick it up).

At the end, report a summary in Dutch, understandable for a technical PO, concise and to the point: which items shipped (with commit hashes), which are blocked and why, anything deferred or worth the PO's attention. No long prose.
```
