# Handoff prompt — BRDG-488 → BRDG-487 → BRDG-489

Paste the block below into a fresh Claude Code thread in the `valk-command` project to run autonomously.

```text
You are implementing three Epic Writer / writer stories in this order: BRDG-488 (simplify phases), then BRDG-487 (Epic Writer polish round 2), then BRDG-489 (clear chat), end-to-end, AUTONOMOUSLY, one after another, on the `dev` branch of the Bridge (valk-command) project. This prompt is your standing approval to implement all of them without pausing to ask "shall I start?" per item. Commit your own work as you go, track progress, archive finished stories, and only advance once the current item is genuinely green. Keep going until all are done or you hit a real blocker.

BEFORE YOU START — clean baseline (mandatory): earlier Epic Writer work (BRDG-484/485/486) may still leave uncommitted changes in the tree. Run `git status`. If it is dirty with changes you did not make, STOP and report rather than building on a half-applied tree. Proceed only when `dev` is clean and the FULL suite is green.

Read first: `CLAUDE.md` (project + global rules); each story file in full before you start it; `docs/architecture/ui-primitives.md` (shared TicketRefPill, sprint representation, ToastCard - for BRDG-487); `docs/architecture/story-writer.md` (chat, panes/apps, draft flow - for BRDG-487 and BRDG-489); `docs/architecture/optimistic-updates.md` (BRDG-487's breakdown reorder is a persisted-order edit); `docs/user-stories/completed/BRDG-479-epic-writer-advance-to-breakdown.md` (the breakdown skill dispatch + phase gating that BRDG-488 must NOT break); `docs/user-stories/completed/BRDG-484-epic-writer-layout-navigation.md` (the layout/view model BRDG-487 builds on).

Scope & order (do them in THIS order):
1. BRDG-488 — simplify Epic Writer phases — `docs/user-stories/BRDG-488-epic-writer-simplify-phases.md`. Drop the `refine` phase and rename `detail` -> `refine` so the rail is Feed/Discovery/Breakdown/Refine/Sprints (Breakdown = titles+bullets, Refine = full body + AC). Touches `src/types/epic-writer.ts`, `src/lib/story-writer-messages.ts` (the `phase === "detail"` skill gates + `epicPhaseUsesBreakdownSkill`), `src/components/epic-writer/EpicWriterLayout.tsx` (`handleSelectPhase` phase->view mapping), plus a data migration for existing sessions (old `detail` -> `refine`, old `refine` -> `breakdown`). DO THIS FIRST so the later layout/context edits build on the final phase names.
2. BRDG-487 — Epic Writer misc improvements round 2 — `docs/user-stories/BRDG-487-epic-writer-misc-improvements-2.md`. Ten items: bigger breakdown font, expand/compact cards, chat-as-toggleable-app, phase-rail alignment to the header logo, strip `<story-detail>` with an attribute-tolerant regex (follow-up to BRDG-478 #5), fix editor `<p>` spacing (carefully, without over-spacing `<p>` elsewhere), make the story editor scroll, editable draft whose edits reach the chat/AI context, card Jira-key as TicketRefPill + standard sprint styling, and breakdown drag-to-reorder. Files include `src/components/epic-writer/ChildStoryCard.tsx`, `BreakdownBoard.tsx`, `EpicWriterLayout.tsx`, `EpicAppsMenu.tsx`, `src/components/story-writer/ChatMessageParts.tsx`, the story editor component, and `src/lib/story-writer-messages.ts` (`buildEpicContext` for the editable-draft-into-chat item).
3. BRDG-489 — clear chat (Story Writer + Epic Writer) — `docs/user-stories/BRDG-489-clear-chat-story-and-epic-writer.md`. A "Clear chat" button + a `/clear` compose command that clears the conversation's messages but KEEPS the session, draft, and breakdown cards. Touches the shared `src/components/story-writer/StoryWriterChat.tsx` (button + `/clear` interception) and the message DELETE on both routes (`/api/tickets/[key]/story-writer/messages`, `/api/epics/[key]/writer/messages` - add a clear-all variant that does not touch session/drafts/cards). Mostly independent of 1 and 2.

Couplings & cautions (respect exactly):
- SEQUENTIAL, 488 before 487: they both edit `EpicWriterLayout.tsx` AND `src/lib/story-writer-messages.ts`. Finish 488 (green + committed + archived) before starting 487, and re-run the FULL suite between them. Never parallel.
- 488 must NOT regress BRDG-479: it only RENAMES the phase; the breakdown still dispatches the `break-down-epic` skill as a fresh session and the dedup fix stays. Ensure `epicPhaseUsesBreakdownSkill` still returns true for the renamed `refine` (and all breakdown-family phases), and the `<story-detail>` gate now fires on `refine`.
- 487 #5 (`ChatMessageParts.tsx` strip) and 489 (`StoryWriterChat.tsx`) both live in the SHARED chat used by the Story Writer and the Epic Writer. After each, verify BOTH writers still render/behave (open `/tickets/<key>/write` and `/epics/<key>/write`).
- 487 #6 (`<p>` spacing) and #9 (TicketRefPill) touch SHARED styles/components - verify no regression in the chat, ticket detail, and draft previews.
- 488's migration: no active session may be left on a removed phase value (it would fail `isEpicWriterPhase` and silently fall back to Feed).

The loop for each item:
1. Read the item. Follow any "Open questions"/recommended default (e.g. BRDG-487 #8: include the edited local draft in the AI context; BRDG-488 migration mapping) unless it proves wrong/blocking once you're in the code.
2. Implement it. You may use the `/implement-story` command as the per-story harness. These are features, polish, and one small refactor (BRDG-488): preserve existing behaviour unless the item says otherwise; the tests are your guardrail; do not change behaviour beyond scope.
3. If anything visual changes, invoke the `frontend-design` skill first (global rule). BRDG-487 is heavily visual.
4. Write/extend co-located `*.test.ts(x)` tests for every change.
5. Verify it ACTUALLY works (the bar to advance — see "Definition of done").
6. Commit (conventional commit referencing the item ID; English; no emojis; NO "Co-Authored-By"). Stage EXPLICIT paths only — never `git add -A`/`.` (the tree may carry unrelated parallel work, including some deleted story docs — never stage those). Several commits per item is fine.
7. Tick the item's checkboxes as you satisfy them; when all are met, add a short "## Status" run note at the top and archive the story: move it to `docs/user-stories/completed/` and commit `chore: archive {ID} as completed`.
8. Only then move to the next item.

Definition of done (must ALL hold before advancing):
- `npm run lint`, `npm run typecheck`, `npx vitest run` (FULL suite), and `npm run build` all green.
- The item's acceptance criteria are met.
- It is E2E-verified in the REAL running app — not just unit tests:
  - UI items: drive the affected view in Chrome via the available browser tools, confirm the behaviour and that the console shows no new errors. Navigate from the sprint board (open the epic writer / a ticket's story writer), not by direct URL (direct URLs hit the Clerk redirect). Use the `verify` or `validate-ui` skill. `VPL-47279` is a real epic with an active writer session, cards, and a draft.
  - Backend items (488 migration, 489 clear route): trigger the real route against the running dev server and confirm correct behaviour + no server errors.

Hard rules (non-negotiable):
- TESTS: `npx vitest run` in the FOREGROUND, ONE process at a time, no pipes, no background, no sleep+cat polling. 16GB machine — concurrent vitest thrashes swap. A postToolUse hook may auto-run tests after edits; let it finish, don't overlap it.
- Run lint + typecheck + test + build before EVERY commit.
- DEV SERVER on port 3101 for Chrome checks (prod is 3100): `curl -s localhost:3101` to see if it's up before starting; never start a second instance; never background it with `&`. After any `npm run build`, RESTART it (`lsof -ti:3101 | xargs kill -9 2>/dev/null` then `npm run dev`).
- BRANCHES: do NOT create/switch branches (a PreToolUse hook blocks it). Commit directly to `dev`. No PR unless asked.
- Conventional commits, English only, no emojis, no "Co-Authored-By". Update `/docs` when behaviour/architecture changes.

Chrome / auth: the app is Clerk-gated. First check existing browser tabs and reuse an already-authenticated Bridge tab if one is open; otherwise use the development auth bypass (`GET /api/dev/bypass` on the dev server; check `src/middleware.ts` and `src/app/api/dev/` / `src/app/dev/` — a dev-only httpOnly cookie). If neither works, that's a valid reason to ask the PO to log in once in the tab you're driving. Never trigger native alert/confirm dialogs (they freeze the automation).

Ask the PO sparingly: decide for yourself using the item's recommended defaults and the tests. Only ask when something is genuinely unverifiable by tests or Chrome (a subjective product/visual call, a wrong default that changes scope, or you can't authenticate Chrome). BATCH questions into one message; keep working on anything unblocked; never ask permission to start/continue.

Pause, don't thrash: if after a couple of honest attempts an item won't go green, or a shared-file change (EpicWriterLayout, story-writer-messages, ChatMessageParts, StoryWriterChat, prose styles) regresses unrelated tests and the fix is non-obvious, STOP that item, leave the tree committed-or-clean (never a half-applied broken edit), note where you're stuck, and continue with the next INDEPENDENT item (489 is independent of 487/488). Report blocked items at the end. Never loop indefinitely on the same failure.

When every item is done: archive this handoff prompt itself — `git mv docs/prompts/2026-07-06-epic-writer-487-488-489.md docs/user-stories/completed/` and commit `chore: archive handoff prompt epic-writer-487-488-489`. Skip this if any item is still blocked (leave the prompt in place so the next run can pick it up).

At the end, report a summary in Dutch, understandable for a technical PO, concise and to the point: which items shipped (with commit hashes), which are blocked and why, anything deferred or worth the PO's attention. No long prose.
```
